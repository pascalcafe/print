{
  "name": "@easyprint/prisma",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node -e \"process.exit(0)\"",
    "lint": "node -e \"process.exit(0)\"",
    "typecheck": "node -e \"process.exit(0)\"",
    "generate": "pnpm exec prisma generate --schema ./schema.prisma",
    "migrate:dev": "node --env-file=.env ./scripts/release-prisma-lock.mjs && pnpm exec prisma migrate dev --schema ./schema.prisma",
    "migrate:deploy": "node --env-file=.env ./scripts/release-prisma-lock.mjs && pnpm exec prisma migrate deploy --schema ./schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "pg": "^8.13.3"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  }
}
