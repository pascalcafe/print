DATABASE_URL="postgresql://easyprint:easyprint@postgres:5432/easyprint?schema=public"
PORT=4000
NEXT_PUBLIC_API_URL="http://localhost:4000/api"
JWT_SECRET=""
JWT_EXPIRES_IN="8h"

# Redis (optional) - use only if backend/worker need it
# example managed Redis: redis://:<password>@host:port
REDIS_URL="redis://redis:6379"

# RabbitMQ (optional) - use only if worker or queues are used
# example: amqp://user:pass@rabbitmq:5672
RABBITMQ_URL="amqp://easyprint:easyprint@rabbitmq:5672"
